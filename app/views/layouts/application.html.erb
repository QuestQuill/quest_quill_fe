<!DOCTYPE html>
<html>
  <head>
    <link href='https://fonts.googleapis.com/css?family=Inter:500' rel='stylesheet'>
    <title>QuestQuill</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="container-fluid">
      <nav class="navbar d-flex justify-content-sm-between justify-content-start">
        <div class="home-link d-flex" >
          <%= link_to "QuestQuill", root_path, class: 'd-flex align-items-center me-2'%> <!-- me-2 for right margin per bootstrap v5 gem docs-->

          <div class="logo">
            <%= image_tag "logo.png" %>
          </div>
        </div>
          <% if current_user %>
          <div class="generator-button">
            <%= button_to "+ New Campaign", "/users/#{params[:id]}/campaigns/new" , method: :get, class: "d-flex" %>
          </div>
          <div class="logout d-flex justify-content-end">
            <%= link_to  "Log Out", "/", data: { turbo_method: :delete } %>
          </div>
          <% end %>
      </nav>
      <% flash.each do |type, msg| %>
        <div class="alert-info">
          <%= msg %>
        </div>
      <% end %>
      <%= yield %>
      
    </div>
  </body>
</html>
